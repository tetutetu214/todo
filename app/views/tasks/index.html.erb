<h1>タスク一覧</h1>
<table>
  <body>
    <% @tasks.each do |task| %>
      <ul>
        <li><%= task.state %></li>
        <li><%= task.task %></li>
        <li><%= task.limit_date %></li>
      </ul>
      <%= link_to '追加', tasks_path %>
    <% end %>
  </body>
</table>